# Lanuguage set to Go
language: go

# OS setup
os: linux
dist: xenial

# Go versions
go:
- 1.12.x
- 1.13.x
- 1.14.x
- 1.15.x

# Make git commands silent
git:
  quiet: true

# Install all dependencies
install:
- go get -v -t -d ./...

jobs:
  include:
  - name: 🧪Tests
    script:
    - go get -v -t -d ./...
    - go build .
    - go test -v -race -coverprofile=coverage.txt -covermode=atomic

after_success:
- bash <(curl -s https://codecov.io/bash)

script: 

